


  # useMemo
    - useMemo is a react hook that lets you cache the result of calculation between re-renders
    - If we have some heavy operations in our component and every time the state changes those operations will happen
    - We can memoize the operations